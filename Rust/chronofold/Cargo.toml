[package]
name = "chronofold"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1.50", features = ["full"] }
tower-http = { version = "0.5", features = ["fs"] }
